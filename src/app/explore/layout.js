export const metadata = {
    title: 'Explore Places - TryToFindEverything',
    description: 'Search and discover hotels, restaurants, bars, guesthouses, car rentals, and tours across the world.',
};

export default function ExploreLayout({ children }) {
    return children;
}
